#include <stdio.h>
#include <utility>
using namespace std;
int n, total_time, a,b;
int time[1001], score[1001];
pair<int, int> a;

int main() {
	scanf("%d %d", &n, &total_time);
	for (int i = 0; i < n; i++) {
//		scanf("%d %d", &a, &b);
		scanf("%d %d", &time[i], &score[i]);
		
	}
}

