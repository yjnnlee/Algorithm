#include <stdio.h>
#include <utility> //pair
#include <vector>
#include <queue> 
using namespace std;

int a, b;
pair<vector<int>, vector<int>> pv;
vector<int> v;
vector<pair<int, int>> vp;
queue<int> q;

int main() {
	q.push(10);
	q.push(20);
	q.push(30);

	q.front();
	
}